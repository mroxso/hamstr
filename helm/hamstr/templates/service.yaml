apiVersion: v1
kind: Service
metadata:
  name: {{ include "hamstr.fullname" . }}
  labels:
    {{- include "hamstr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hamstr.selectorLabels" . | nindent 4 }}
