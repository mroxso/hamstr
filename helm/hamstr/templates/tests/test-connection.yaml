apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hamstr.fullname" . }}-test-connection"
  labels:
    {{- include "hamstr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hamstr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
